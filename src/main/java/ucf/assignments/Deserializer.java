package ucf.assignments;

import com.google.gson.Gson;

import java.io.File;
import java.util.ArrayList;

public class Deserializer {
    ArrayList<Item> deserializeTSV() {
        // use FileReader to read the items in the .txt file
        // parse each line of data into a new Item object
        // add the new Item object to the items ObservableArrayList
        // return the ObservableArrayList
        return new ArrayList<Item>();

    }
    ArrayList<Item> deserializeHTML() {
        // use FileReader to read the items in the .txt file
        // parse each line of data into a new Item object
        // add the new Item object to the items ObservableArrayList
        // return the ObservableArrayList
        return new ArrayList<Item>();
    }
    ArrayList<Item> deserializeJSON() {
        // use FileReader to read the items in the .txt file
        // parse each line of data into a new Item object
        // add the new Item object to the items ObservableArrayList
        // return the ObservableArrayList
        return new ArrayList<Item>();
    }
}
